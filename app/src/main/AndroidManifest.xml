<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vaishnavi.todo">

    <application
        android:name=".helper.CustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddTaskActivity"
            android:parentActivityName=".ToDo2Activity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ToDo2Activity" />
        </activity>
        <activity
            android:name=".ViewTaskActivity"
            android:parentActivityName=".ToDo2Activity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ToDo2Activity" />
        </activity>
        <activity
            android:name=".EditTaskActivity"
            android:parentActivityName=".ToDo2Activity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ToDo2Activity" />
        </activity>
        <activity android:name=".ToDo2Activity"></activity>
    </application>

</manifest>